<h3>Edit Artist</h3>

<%= form_for(@artist) do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %><br>
  <%= f.label :bio %><br>
  <%= f.text_area :bio %><br>
  <%= hidden_field_tag :authenticity_token, form_authenticity_token %>
  <%= f.submit %>
<% end %>
